# mediaschema mono-consolidation — Implementation Plan (single `media.v1` file)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`. Self-authored, executed autonomously/continuously per explicit user directive (this redesign + autonomous continuous execution were directed by the user) — **no per-task human check-in, no user-approval gate**.

**Goal:** Collapse the reviewed 3-package split — `media.v1` (SP0+SP1, `proto/media/v1/types.proto`), `findit.db.v1` (SP2, `proto/findit/db/v1/database.proto`), `findit.net.v1` (SP3, `proto/findit/net/v1/network.proto`) — into a **single proto package `media.v1` in a single proto file `proto/media/v1/types.proto`**, with **no cross-package references** (everything same-package), preserving every message/field/enum/tag exactly. `mediatime.v1` stays the extern crate, **UNCHANGED** (`extern_path` → `::mediatime`, never generated).

**Architecture:** This is a **structural refactor of already-reviewed content**, not a content redesign. SP0 & SP1 are already `package media.v1;` in the single `proto/media/v1/types.proto` — **NOT touched by this plan**. The consolidation rewrites only the `sp2-database` and `sp3-network` branches:

1. Append the FULL body of `database.proto` then `network.proto` (message/enum blocks + section comments, in source order) into `proto/media/v1/types.proto` under the existing `package media.v1;`. Delete `database.proto` / `network.proto` and prune the now-empty `proto/findit/...` dirs. One `syntax`/`package`/`import` header survives (the SP0/SP1 one already in `types.proto`, which already has `import "mediatime/v1/mediatime.proto";`).
2. Two message renames (the only same-package collisions): `findit.db.v1.MediaKind` → **`DbMediaKind`**; `findit.net.v1.FailedFile` → **`NetFailedFile`**. SP1's `media.v1.MediaKind` (oneof) and SP2's `FailedFile` keep their names.
3. Every cross-package FQN qualifier (`media.v1.<T>`, `findit.db.v1.<T>`) in the merged SP2/SP3 content drops to a bare `<T>` same-package ref. `mediatime.v1.<T>` refs are UNCHANGED (extern).
4. `xtask/src/main.rs` `.files()` back to the SINGLE entry `proto/media/v1/types.proto`.
5. Regenerate: only `src/generated/media.v1.*` (+ `mod.rs`) survive; delete the stale `findit.db.v1.*` / `findit.net.v1.*` generated files.
6. `src/lib.rs`: collapse the three `pub use` blocks into ONE `pub use generated::media::v1::{ … };` (alphabetically merged, named not glob). `DbMediaKind`/`NetFailedFile` become plain idents (their `as` alias lines deleted — the names are now real). Oneof companions re-point under `generated::media::v1::…`.
7. `tests/roundtrip.rs`: bodies barely change (the crate-root names `DbMediaKind`/`NetFailedFile` already existed as the alias names, so test constructions resolve unchanged). The `use mediaschema::{…}` import block is unchanged by the merge (no idents added/removed).

**Tech Stack:** buffa/buffa-types/buffa-build `0.6` + mediatime `0.1.6` (crates.io), protoc 34.0, features `json`/`arbitrary`/`quickcheck`.

**Spec:** `docs/superpowers/specs/2026-05-17-mediaschema-full-migration-design.md` (§2 locked decisions; §6 SP2 mapping; §7 SP3 mapping). The field/enum/tag content of §1–§7 is the authoritative anchor and is **preserved verbatim**; only the package-name prose and the two collision names change.

**Branch model (Q2 = 2a — rebuild in place):** `sp1-common`@`1170313` is UNCHANGED (already mono). `sp2-database` is rebuilt = `sp1-common` + the SP2 docs commits (reconciled for mono) + ONE consolidation commit (SP2 slice of steps 1–7). `sp3-network` is rebuilt = the NEW `sp2-database` + the SP3 docs commits (reconciled) + ONE consolidation commit (SP3 slice of steps 1–7). Both force-pushed (`--force-with-lease`); PRs #4 (`sp2-database`→`sp1-common`) / #5 (`sp3-network`→`sp2-database`) auto-update — **bases unchanged**.

---

## The Q1 / Q2 decisions (decided by the user)

- **Q1 — target shape:** ONE package `media.v1`, ONE file `proto/media/v1/types.proto`, NO cross-package references (all same-package). `mediatime.v1` remains the only extern (→ `::mediatime`, never generated). The 2 same-package name collisions are resolved by renaming the proto definitions: `findit.db.v1.MediaKind` → `DbMediaKind`, `findit.net.v1.FailedFile` → `NetFailedFile`.
- **Q2 — branch strategy = 2a (rebuild in place):** keep the stacked-PR topology (`#1` `sp0-foundation`→`main`, `#3` `sp1-common`→`sp0-foundation`, `#4` `sp2-database`→`sp1-common`, `#5` `sp3-network`→`sp2-database`); rewrite the `sp2-database` and `sp3-network` histories in place (docs commits + one consolidation commit each) and `--force-with-lease` push; PR bases are unchanged so #4/#5 update automatically. `sp0-foundation`/`sp1-common`/#1/#3 are untouched.

### Current verified state (read before executing)

- Local & origin HEADs match: `sp0-foundation`@`0c3eeff`, `sp1-common`@`1170313`, `sp2-database`@`0ddb1cc`, `sp3-network`@`9dbdec6`. PRs #1/#3/#4/#5 OPEN on `Findit-AI/mediaschema`.
- `sp1-common`@`1170313`: `proto/media/v1/types.proto` (`syntax="proto3"; package media.v1;` + `import "mediatime/v1/mediatime.proto";`), 46 messages + 5 enums (incl. SP1's `MediaKind` oneof). No `findit/...` proto. **Untouched by this plan.**
- `sp2-database`@`0ddb1cc` = `sp1-common` + docs commits `394e470` ("docs: SP2 (database) design — §6 findit.db.v1 mapping + batches") and `8820031` ("docs: SP2 implementation plan (Task0 codegen scaffolding + 10 batches)") + 11 feature commits `3136668`(batch0) … `0ddb1cc`(batch10). Adds `proto/findit/db/v1/database.proto` (`package findit.db.v1;`, 43 messages incl. `Sp2CodegenSmoke` + 12 enums incl. `MediaKind`), `src/generated/findit.db.v1.*` (3 files), and the SP2 `src/lib.rs` block.
- `sp3-network`@`9dbdec6` = `sp2-database` + docs commits `3080f17` ("docs: SP3 (network) design — §7 findit.net.v1 mapping + batches"), `e54be45` ("docs: SP3 implementation plan (Task0 3-way codegen scaffolding + 9 batches)"), `e9b502c` ("docs: reconcile SP3 §7 message-count prose with §7.5 mapping (48 msgs / 0 enums)"), `594d9d8` ("docs: reconcile SP3 §7.8/plan — Response.error arm 19999->20000…") + 11 feature commits `46d7b11`(batch0) … `9dbdec6`(batch9). Adds `proto/findit/net/v1/network.proto` (`package findit.net.v1;`, 49 messages incl. `Sp3CodegenSmoke` + `FailedFile`, 0 enums), `src/generated/findit.net.v1.*` (3 files), and the SP3 `src/lib.rs` block.

### Pre-computed transform inventory (computed while authoring — anchors the steps; do not re-derive)

**Collision survey (definitive — confirmed by sorting every `^message`/`^enum` name across all 3 protos): EXACTLY two names appear in 2+ packages — `MediaKind` and `FailedFile`. No other top-level message/enum name collides.** Therefore exactly two renames; no others.

- **Rename #1: `MediaKind` (the SP2 `findit.db.v1` tri-state enum) → `DbMediaKind`.** Use-sites:
  - proto **definition** `proto/findit/db/v1/database.proto`: `enum MediaKind { MEDIA_KIND_UNSPECIFIED=0; MEDIA_KIND_VIDEO=1; MEDIA_KIND_AUDIO=2; }` (lines 36–40). Enum **values** keep their `MEDIA_KIND_*` names (only the type name changes).
  - proto **field use site (1):** `database.proto:396` — `MediaKind kind = 2;` in `message Media`.
  - proto **comment** `database.proto:34` — "DISTINCT from media.v1.MediaKind …" (rewritten in the FQN-drop step; the 2 `media.v1.MediaKind` occurrences at `database.proto:14` & `:34` are comment-only and refer to SP1's kept type — they become bare `MediaKind` in prose with a clarifying note, see M2 Step 4).
  - **generated** (via regen, not hand-edit): `src/generated/media.v1.types.rs` will contain the renamed `pub struct DbMediaKind` etc. (the old `findit.db.v1.database.rs` had 17 `MediaKind` occurrences — all regenerated under the new name).
  - **`src/lib.rs`:** the `pub use generated::findit::db::v1::MediaKind as DbMediaKind;` line (lib.rs:57) — the `as` alias is **removed**; `DbMediaKind` becomes a plain ident in the single merged brace list (it is now the real generated name).
  - **`tests/roundtrip.rs`:** already references `DbMediaKind` (lines 1004–1005, 1382, 1398) — **no test-body change** (the name it used as an alias is now the real name). The import block already imports `DbMediaKind` (line 13) — unchanged.
  - **NOT renamed:** SP1's `media.v1.MediaKind` (the video/audio-format oneof, `proto/media/v1/types.proto:72`) keeps its name; its tests (roundtrip.rs:150–164) and `MediaKindKind` companion are unchanged.
- **Rename #2: `FailedFile` (the SP3 `findit.net.v1` message) → `NetFailedFile`.** Use-sites:
  - proto **definition** `proto/findit/net/v1/network.proto:118` — `message FailedFile { uint32 kind=1; media.v1.Location location=2; media.v1.ErrorInfo error=3; uint32 error_status=4; uint32 index_status=5; }`.
  - proto **field use sites (2):** `network.proto:216` — `repeated FailedFile failed_files = 6;` in `message GetLocationStatsResponse`; `network.proto:222` — `repeated FailedFile failed_files = 2;` in `message FailedFilesResponse`.
  - proto **comments** `network.proto:19,20,117` reference `findit.db.v1.FailedFile` (the kept SP2 type) — these are prose distinguishing the two; reword to "`DbFailedFile`-vs-`NetFailedFile`"-style same-package prose (see M3 Step 4).
  - **generated** (via regen): `src/generated/media.v1.types.rs` will contain `pub struct NetFailedFile`.
  - **`src/lib.rs`:** the `pub use generated::findit::net::v1::FailedFile as NetFailedFile;` line (lib.rs:92) — the `as` alias is **removed**; `NetFailedFile` becomes a plain ident in the merged brace list.
  - **`tests/roundtrip.rs`:** already references `NetFailedFile` (lines 2682, 2691, 2699, 2991, 3015, 3542) — **no test-body change**. Import block already imports `NetFailedFile` (line 29) — unchanged.
  - **NOT renamed:** SP2's `findit.db.v1.FailedFile` (the DB record, `proto/findit/db/v1/database.proto:351`) keeps its name; its tests (roundtrip.rs:1238–1239) and the crate-root `FailedFile` re-export are unchanged.

**Cross-package → bare-ref rewrite sites (the FQN-drop rule):** in the merged SP2/SP3 content, replace every `media.v1.<T>` and every `findit.db.v1.<T>` (both field-type references **and** prose/comment references) with the bare `<T>`. **`mediatime.v1.<T>` is left exactly as-is (extern).** Precise inventory:

- In `database.proto` (SP2 slice): **`media.v1.<T>` — 57 references** across 25 distinct types: `ErrorInfo`×9, `Detection`×7, `TrackTime`×6, `Dimensions`×4, `CodecId`×3, `VideoFormat`×2, `SaliencyRegion`×2, `ObjectDetection`×2, `MoodDetection`×2, `LightingDetection`×2, `ColorDetection`×2, `ClassificationDetection`×2, `ActionDetection`×2, and `TextDetection`/`SubjectDetection`/`Location`/`HumanAnalysis`/`HorizonInfo`/`FeaturePrint`/`EmotionDetection`/`DocumentSegment`/`BarcodeDetection`/`AudioFormat`/`AnimalAnalysis`/`Aesthetics` ×1 each. (Plus the 2 comment-only `media.v1.MediaKind` strings at lines 14 & 34 — these become bare `MediaKind` in prose with a clarifying note; **no SP2 field references `media.v1.MediaKind`**.) `mediatime.v1.<T>` in SP2 = `TimeRange`×4 + `Timebase`×3 — UNCHANGED. `findit.db.v1.` qualifiers do not occur inside `database.proto` itself.
- In `network.proto` (SP3 slice): **`media.v1.<T>` — 25 references**: `Location`×10, `ErrorInfo`×6, `WatchedLocation`×2, `VolumeMeta`×2, `Tag`×2, `Dimensions`×2, `LocationTarget`×1. **`findit.db.v1.<T>` — 5 field references**: `VideoMeta`×2 (`Sp3CodegenSmoke.video_meta` line 35; `BrowseItem.meta` line 84), `Video`×1 (`GetIndexedFileResponse.video` line 240), `Scene`×1 (`GetIndexedFileResponse.scenes` line 241) — plus 3 comment-only `findit.db.v1.…` strings (lines 20, 117, 237) reworded as same-package prose. `mediatime.v1.TimeRange`×4 — UNCHANGED.
- **Post-condition:** after the rewrite, `grep -nE 'findit\.db\.v1\.|findit\.net\.v1\.|media\.v1\.' proto/media/v1/types.proto` returns ONLY lines that are `mediatime.v1.` extern refs — i.e. **zero** `media.v1.`/`findit.db.v1.`/`findit.net.v1.` qualifiers remain (the package is now implicit/same-package).

**How to find every site mechanically (the grep recipe, run in `/Users/user/Develop/findit-studio/mediaschema`):**

```
# every cross-package ref to rewrite (field + comment), per source file, BEFORE merge:
grep -nE 'media\.v1\.[A-Za-z]'      proto/findit/db/v1/database.proto    # SP2 media.v1 refs → bare
grep -nE 'media\.v1\.[A-Za-z]'      proto/findit/net/v1/network.proto    # SP3 media.v1 refs → bare
grep -nE 'findit\.db\.v1\.[A-Za-z]' proto/findit/net/v1/network.proto    # SP3 findit.db.v1 refs → bare
grep -nE 'mediatime\.v1\.[A-Za-z]'  proto/findit/db/v1/database.proto proto/findit/net/v1/network.proto  # MUST stay as-is
# rename sites:
grep -nE '(^|[^.])\bMediaKind\b'  proto/findit/db/v1/database.proto     # SP2 MediaKind def+use → DbMediaKind
grep -nE '(^|[^.])\bFailedFile\b' proto/findit/net/v1/network.proto     # SP3 FailedFile def+use → NetFailedFile
```

The mechanical rule: `media.v1.X` → `X`, `findit.db.v1.X` → `X`, `mediatime.v1.X` → `mediatime.v1.X` (no change). Then in the SP2 slice `MediaKind` (the bare enum name, NOT inside `media.v1.MediaKind`) → `DbMediaKind`; in the SP3 slice `FailedFile` (bare, NOT inside `findit.db.v1.FailedFile`) → `NetFailedFile`. **Apply renames AFTER the FQN-drop**, so `media.v1.MediaKind` is already bare `MediaKind` (SP1's kept type) and is NOT caught by the SP2 `MediaKind→DbMediaKind` pass (the SP2 pass runs only over the appended SP2 block, and SP2 has no `media.v1.MediaKind` field — the only `MediaKind` tokens in the SP2 block are its own enum def+`Media.kind` use; the 2 comment strings are handled explicitly).

---

## Conventions (all tasks)

**C1 — Preserve, do NOT re-derive.** The §6/§7 message/field/enum/tag content is already authored and reviewed. **Every field name, field number, enum value, enum number, `oneof` arm + tag, `reserved` line, and intent comment is preserved byte-identical** through the merge. The ONLY edits to message bodies are: (a) drop the `media.v1.`/`findit.db.v1.` qualifier prefix from type references; (b) the 2 type-name renames at their def + use sites. No tag is renumbered, no field added/removed, no message added/removed (SP3's `Response.error = 20000` stays 20000; SP2's `reserved` lines stay; the `Sp2CodegenSmoke`/`Sp3CodegenSmoke` fixtures stay as permanent regression guards — their cross-package refs simply become same-package). If any step would change content beyond (a)/(b), STOP — that is a bug in this plan, not an instruction to improvise.

**C2 — Verification block** (run after each consolidation commit; cached registry, no network). For every `cargo`/`gh` invocation use the Bash tool with **`dangerouslyDisableSandbox: true`** and prefix with `env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo`:

```
cd /Users/user/Develop/findit-studio/mediaschema
env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo cargo build
env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo cargo build --no-default-features
env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo cargo build --features json
env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo cargo build --features arbitrary
env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo cargo build --features quickcheck
env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo cargo build --all-features
env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo cargo test --features quickcheck,json 2>&1 | grep "test result:"
env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo cargo run -p xtask -- gen && git diff --exit-code -- src/generated && echo GEN_CLEAN
```

All 6 builds PASS; all tests pass; `GEN_CLEAN`. If a cargo cmd hits a registry/network error, retry the SAME command (still `dangerouslyDisableSandbox: true`; deps cached under `/Users/user/Develop/findit-studio/.cargo`).

**C3 — Test-count invariant (structural refactor ⇒ count is preserved exactly).** This is a structural refactor, not a content change, so the **full prior integration test count must still pass unchanged**: SP1 = 19 tests; SP2 adds +21 → 40; SP3 adds +20 → 60. The expected final figure on the rebuilt `sp3-network` is **60 integration tests** (the same 60, since no test is added or removed by the merge). On the rebuilt `sp2-database` the figure is **40** (SP1 19 + SP2 21). Record the exact `test result:` line each time and assert it equals the pre-rebuild count for that branch (capture the baseline first: `git stash`-free, run `cargo test --features quickcheck,json 2>&1 | grep "test result:"` on the un-rebuilt branch and note the integration-suite `roundtrip` count; it must match post-rebuild). A count drift = a real regression; investigate, do not paper over.

**C4 — Grep guards** (run after each consolidation commit; all must hold):

```
cd /Users/user/Develop/findit-studio/mediaschema
# (a) NO old package qualifiers anywhere (proto / generated / lib / tests):
grep -rnE 'findit\.db\.v1|findit\.net\.v1' proto src tests ; echo "exit=$?"   # expect: no matches, exit=1
grep -rn  'findit::db::v1\|findit::net::v1' src tests        ; echo "exit=$?"   # expect: no matches, exit=1
# (b) NO deleted proto files / NO stale generated files:
test ! -e proto/findit/db/v1/database.proto && test ! -e proto/findit/net/v1/network.proto && echo NO_OLD_PROTO
ls src/generated | grep -E '^findit\.' ; echo "exit=$?"                        # expect: no matches, exit=1
# (c) exactly ONE package line in the unified proto:
grep -cE '^package ' proto/media/v1/types.proto                                # expect: 1
grep -cE '^syntax '  proto/media/v1/types.proto                                # expect: 1
# (d) renamed types present, old colliding names gone at the def:
grep -nE '^enum DbMediaKind \{'    proto/media/v1/types.proto                   # expect: 1 hit
grep -nE '^message NetFailedFile \{' proto/media/v1/types.proto                # expect: 1 hit
grep -cE '^enum MediaKind \{'      proto/media/v1/types.proto                   # expect: 0  (SP1 MediaKind is a `message`, not `enum`)
grep -cE '^message MediaKind \{'   proto/media/v1/types.proto                   # expect: 1  (SP1's kept oneof message)
grep -cE '^message FailedFile \{'  proto/media/v1/types.proto                   # expect: 1  (SP2's kept DB record)
# (e) only mediatime.v1 qualifiers remain in the proto:
grep -nE 'media\.v1\.|findit\.db\.v1\.|findit\.net\.v1\.' proto/media/v1/types.proto ; echo "exit=$?"  # expect: no matches, exit=1
grep -cE 'mediatime\.v1\.' proto/media/v1/types.proto                          # expect: 11 (4 TimeRange + 3 Timebase from SP2 + 4 TimeRange from SP3)
```

`exit=1` from `grep` means "no match" (success here). Note guard (d): SP1's surviving `MediaKind` is `message MediaKind {` (the oneof), and SP2's surviving `FailedFile` is `message FailedFile {` — both must still be present exactly once; only the renamed-away `enum MediaKind`/`message NetFailedFile`-was-`FailedFile` collisions are gone.

**C5 — Force-push safety.** Both branch rewrites use `git push --force-with-lease origin <branch>` (never bare `--force`). Local & origin HEADs are confirmed equal at plan start (`sp2-database`@`0ddb1cc`, `sp3-network`@`9dbdec6`); `--force-with-lease` aborts if origin moved underneath. PR bases are NOT changed (#4→`sp1-common`, #5→`sp2-database`), so the PRs update in place.

**C6 — Two-stage review per consolidation task.** After M2's consolidation commit and after M3's, run a two-stage subagent review (stage 1: spec/structure fidelity — *every field/tag/enum preserved, the 2 renames correct at def+all-use, FQNs fully dropped, no content drift*; stage 2: code-quality — *generated drift-clean, lib re-export merged correctly, tests still 40/60, grep guards*). Triage findings rigorously: accept real defects in the refactor; reject out-of-scope / would-introduce-inconsistency / plan-faithful-and-correct items with documented reasoning. M4 adds a final whole-impl capstone review over the rebuilt sp3-network tip.

**C7 — Append order.** When appending SP2 then SP3 bodies into `types.proto`: preserve each source file's internal block order exactly (its `// ── Batch N …` section banners + message/enum blocks top-to-bottom). SP2's whole body is appended first (after SP1's last message `HumanAnalysis`), then SP3's whole body after SP2's last message `AudioFileRecord`. Strip from each appended body ONLY the leading `syntax = "proto3";`, `package …;`, the `import "media/v1/types.proto";` / `import "findit/db/v1/database.proto";` self/cross-package import lines, and the blank lines around them — keep the descriptive header comment block of each (re-titled, see M2/M3 Step 1) and ALL message/enum/section content. The `import "mediatime/v1/mediatime.proto";` already present at the top of `types.proto` covers the extern for the merged content (do NOT duplicate it).

---

## Task M1: Reconcile docs (spec §2/§6/§7 + SP2/SP3 plan headers) for the mono outcome

**Why first:** the consolidation commits (M2/M3) physically supersede the SP2/SP3 plan docs on their rebuilt branches; the reconciled docs must be the version that rides those rebuilds. M1 produces the reconciled doc text on disk now (on the current `sp3-network` checkout, alongside this plan), and M2/M3 re-add the reconciled docs onto the rebuilt branches.

**Files:** `docs/superpowers/specs/2026-05-17-mediaschema-full-migration-design.md`; `docs/superpowers/plans/2026-05-18-mediaschema-sp2-database.md`; `docs/superpowers/plans/2026-05-18-mediaschema-sp3-network.md`. **Do NOT rewrite unrelated content** — §1–§5 message/field tables stay; only package-name prose changes where it asserts the 3-package split, plus a self-correction note in the §6.8/§7.8 style.

- [ ] **Step 1 — Spec §2 + §3 (locked-decision package line).** In `…full-migration-design.md`:
  - §2: the bullet ending "packages `media.v1` (common, extends SP0) + `findit.db.v1` (SP2) + `findit.net.v1` (SP3)" → restate as: "single package `media.v1` in a single file `proto/media/v1/types.proto` for SP0+SP1+SP2+SP3 (the original 3-package split `media.v1`/`findit.db.v1`/`findit.net.v1` was **consolidated into one same-package file per explicit user directive** — no cross-package references; the 2 same-package name collisions `findit.db.v1.MediaKind`→`DbMediaKind` and `findit.net.v1.FailedFile`→`NetFailedFile` are resolved by renaming the proto definitions); `mediatime.v1` remains the sole `extern_path`→`::mediatime`."
  - §3 ("Package & branch layout"): the "Packages:" paragraph asserting "Add **`findit.db.v1`** (SP2) and **`findit.net.v1`** (SP3) as new packages/files" → restate to the single-`media.v1`-file outcome (keep the stacked-branch paragraph unchanged — branches/PR topology is unchanged by Q2=2a).
- [ ] **Step 2 — Spec §6.1 + §6.8 (SP2 package + collision).** In §6.1: the "**New package `findit.db.v1`** in its own file `proto/findit/db/v1/database.proto` (`syntax = "proto3"; package findit.db.v1;`)" sentence and the "buffa resolves the cross-package refs" sentence → restate: SP2's `database` types live in the **single `media.v1` package / `proto/media/v1/types.proto` file** (no separate package/file; no cross-package refs — all `media.v1.*` references are bare same-package after consolidation). In §6.1's "Name disambiguation" call-out and §6.8 #6: restate that `findit.db.v1.MediaKind` was renamed **`DbMediaKind`** (a same-package rename, since both `MediaKind`s now share `media.v1`; SP1's `MediaKind` oneof keeps its name) — reason: user directive consolidating to one package made the package-disambiguation unavailable, so the SP2 enum is renamed at the proto definition. Leave §6.3–§6.5 field/enum/tag tables UNCHANGED.
- [ ] **Step 3 — Spec §7.1 + §7.8 (SP3 package + collision).** In §7.1: the "**New package `findit.net.v1`** in its own file `proto/findit/net/v1/network.proto`" + "three `import`s" + "three-way cross-package" prose → restate: SP3's `network` types live in the **single `media.v1` package / `proto/media/v1/types.proto` file**; no cross-package refs (all `media.v1.*`/`findit.db.v1.*` refs are bare same-package). In §7.1's "Name disambiguation" call-out and §7.8 (add a new numbered item, mirroring §7.8 #11/#12 self-correction style): `findit.net.v1.FailedFile` was renamed **`NetFailedFile`** (same-package rename; SP2's `FailedFile` DB record keeps its name) — reason: user directive. Leave §7.5 mapping tables UNCHANGED.
- [ ] **Step 4 — Spec reconciliation note (mirror §6.8/§7.8 self-correction style).** Append to §6.8 a new item and to §7.8 a new item (continue the existing numbering): a "**mono-consolidation (user directive)**" entry stating: the 3-package split (`media.v1`/`findit.db.v1`/`findit.net.v1`) was consolidated into a single `media.v1` package in `proto/media/v1/types.proto` with no cross-package references, per explicit user directive (a structural refactor — no field/tag/enum/message changed; only the package partition and the 2 collision names); the cross-package codegen mechanism (native `super::`) is no longer exercised by SP2/SP3 (everything is same-package) but `mediatime.v1` remains extern unchanged; the `Sp2CodegenSmoke`/`Sp3CodegenSmoke` fixtures are retained as permanent regression guards (now plain same-package messages). Do not delete the prior cross-package-mechanism prose; add the note as a superseding self-correction (the §6.8 #1 / §7.8 #11 precedent style — "X was stated; the resolution is now Y, here is why").
- [ ] **Step 5 — SP2 plan header (`…sp2-database.md`).** Update the **Goal/Architecture/Spec lines + the "Cross-package codegen mechanism" section + Conventions** to state the mono outcome: SP2 content is authored into the single `proto/media/v1/types.proto` (package `media.v1`), no `proto/findit/db/v1/database.proto`, no cross-package refs (bare same-package), single-entry xtask `.files()`, `findit.db.v1.MediaKind`→`DbMediaKind` rename, `src/lib.rs` collapses into the one merged `media.v1` re-export block. Keep the per-batch field/tag/enum content and Self-Review UNCHANGED (those describe content that is preserved). Add a one-paragraph "Mono-consolidation note" at the top: "Per user directive this plan's output was consolidated — see `2026-05-18-mediaschema-mono-consolidation.md`; SP2 types are now same-package `media.v1` in `proto/media/v1/types.proto`, `MediaKind`→`DbMediaKind`."
- [ ] **Step 6 — SP3 plan header (`…sp3-network.md`).** Same as Step 5 for SP3: single `media.v1` file, no `network.proto`, no three-way cross-package (bare same-package), single-entry xtask, `findit.net.v1.FailedFile`→`NetFailedFile`, merged lib block. Keep per-batch content + Self-Review UNCHANGED. Add the analogous top "Mono-consolidation note".
- [ ] **Step 7 — verify docs internally consistent.** `grep -nE 'findit\.db\.v1|findit\.net\.v1|findit/db/v1|findit/net/v1' docs/superpowers/specs/2026-05-17-mediaschema-full-migration-design.md docs/superpowers/plans/2026-05-18-mediaschema-sp2-database.md docs/superpowers/plans/2026-05-18-mediaschema-sp3-network.md` — every remaining hit must be **either** inside the §6.8/§7.8 self-correction note (quoting the old name to explain the change) **or** an unchanged §6.5/§7.5 mapping-table cell that does not assert the package split (those tables describe fields, not packaging — they are content, preserved). No prose outside the self-correction notes may still assert "new package `findit.db.v1`/`findit.net.v1`" or "cross-package". Two-stage review (C6 stage-1 doc-fidelity only): a subagent diffs M1's doc edits against this plan's directive and confirms no field/tag/enum table cell was altered and the package-split prose is fully reconciled; fix inline.

**Note:** M1 edits land on the working tree of the **currently checked-out `sp3-network`**; they will be **committed as part of M3's docs-commit re-add** (and the spec edits also ride M2 since the spec is shared and present on `sp2-database`). M1 does **not** itself commit (M2/M3 own the commits). After M1, leave the reconciled doc files modified-in-tree; M2 will read the spec edits when it checks out the rebuilt `sp2-database` (so M1's spec edits must be carried across — see M2 Step 0).

**M1 task/step count: 1 task, 7 steps.**

---

## Task M2: Consolidate SP2 — rebuild `sp2-database` (SP2 slice of steps 1–7)

**Goal:** rebuild `sp2-database` = `sp1-common`@`1170313` + the reconciled SP2 docs commits + ONE consolidation commit that applies the SP2 slice of the transform (DbMediaKind rename, SP2 content into `types.proto`, no `findit.db.v1` file/package, single-entry xtask, regen, lib collapse for SP2 idents). `sp1-common` is UNCHANGED.

**Files (in the rebuilt tree):** `proto/media/v1/types.proto` (append SP2 body); delete `proto/findit/db/v1/database.proto` (+ prune dir); `xtask/src/main.rs` (single-entry `.files()`); `src/lib.rs` (merge SP2 idents into the single `media.v1` block); `src/generated/**` (regen → only `media.v1.*` + `mod.rs`); `tests/roundtrip.rs` (verify unchanged-by-merge); the reconciled `docs/...` (spec + SP2 plan).

- [ ] **Step 0 — preserve M1's reconciled docs across the rebuild.** From the current `sp3-network` checkout, copy the three M1-edited doc files to a scratch location OUTSIDE the repo so the branch reset cannot lose them:
  ```
  cd /Users/user/Develop/findit-studio/mediaschema
  mkdir -p /tmp/mono-docs
  cp docs/superpowers/specs/2026-05-17-mediaschema-full-migration-design.md /tmp/mono-docs/
  cp docs/superpowers/plans/2026-05-18-mediaschema-sp2-database.md /tmp/mono-docs/
  cp docs/superpowers/plans/2026-05-18-mediaschema-sp3-network.md /tmp/mono-docs/
  cp docs/superpowers/plans/2026-05-18-mediaschema-mono-consolidation.md /tmp/mono-docs/
  ```
  (Also note the SP2/SP3 design-doc commit *messages* to reuse verbatim: `394e470`/`8820031` for SP2; `3080f17`/`e54be45`/`e9b502c`/`594d9d8` for SP3.)
- [ ] **Step 1 — capture the SP2 baseline test count.** With `sp2-database`@`0ddb1cc` checked out: `git -C /Users/user/Develop/findit-studio/mediaschema checkout sp2-database` then run `env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo cargo test --features quickcheck,json 2>&1 | grep "test result:"` (dangerouslyDisableSandbox:true). Record the integration `roundtrip` suite count — **expected 40** (SP1 19 + SP2 21). This is the post-rebuild target for `sp2-database` (C3).
- [ ] **Step 2 — reset `sp2-database` to `sp1-common`.**
  ```
  git -C /Users/user/Develop/findit-studio/mediaschema checkout sp2-database
  git -C /Users/user/Develop/findit-studio/mediaschema reset --hard sp1-common
  ```
  (`sp1-common`@`1170313` is the unchanged mono base. Verify `git rev-parse HEAD` == `1170313…` and `git log --oneline -1` is the SP1 tip; confirm `proto/findit/` does NOT exist and `proto/media/v1/types.proto` has `package media.v1;`.)
- [ ] **Step 3 — re-add the reconciled SP2 docs commits (2 commits, verbatim messages).** Restore the M1-reconciled spec + SP2 plan from scratch, staged as the original two docs commits (so PR #4's history still shows the design+plan commits, now reconciled):
  - Commit A (the SP2 design doc): write the reconciled spec (`/tmp/mono-docs/2026-05-17-mediaschema-full-migration-design.md` → `docs/superpowers/specs/…`). Because the original `394e470` introduced the §6 design and the original `8820031` introduced the SP2 plan, restore them in two commits:
    ```
    cp /tmp/mono-docs/2026-05-17-mediaschema-full-migration-design.md docs/superpowers/specs/2026-05-17-mediaschema-full-migration-design.md
    git add docs/superpowers/specs/2026-05-17-mediaschema-full-migration-design.md
    git commit -m "$(printf 'docs: SP2 (database) design — §6 media.v1 mapping + batches (mono-consolidated)\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>')"
    ```
  - Commit B (the SP2 plan):
    ```
    cp /tmp/mono-docs/2026-05-18-mediaschema-sp2-database.md docs/superpowers/plans/2026-05-18-mediaschema-sp2-database.md
    git add docs/superpowers/plans/2026-05-18-mediaschema-sp2-database.md
    git commit -m "$(printf 'docs: SP2 implementation plan (mono-consolidated; single media.v1 file)\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>')"
    ```
  (Spec edits include the §7/§7.8 SP3 reconciliation too — that is fine; the spec is one shared file and rides the earliest branch that contains it. The SP3 plan doc is NOT re-added here — it belongs to `sp3-network` and is restored in M3.)
- [ ] **Step 4 — apply the SP2 consolidation transform** (one working-tree edit set, then ONE commit):
  1. **Read** the SP2 proto body from the ORIGINAL `sp2-database`@`0ddb1cc` (it was reset away): `git -C /Users/user/Develop/findit-studio/mediaschema show 0ddb1cc:proto/findit/db/v1/database.proto > /tmp/mono-docs/database.proto.orig`. This is the exact reviewed content.
  2. **Build the SP2 append block** from `database.proto.orig`: take the file from its descriptive header comment (line 5 `// SP2 — findit \`database\` domain…`) onward, BUT (per C7) **drop** lines 1–4 (`syntax`/blank/`package findit.db.v1;`/blank) and the two `import "media/v1/types.proto";` / `import "mediatime/v1/mediatime.proto";` lines (lines 16–17) and the blank line after them. Keep the header comment block (retitle its first line to `// ── SP2: findit \`database\` domain (consolidated into media.v1) ──` and reword the in-comment "REUSE, NEVER REDEFINE … buffa compiles this file together with proto/media/v1/types.proto … findit.db.v1.MediaKind is a DIFFERENT type from media.v1.MediaKind" sentence to: "Same-package (media.v1) since the mono-consolidation; the SP2 \`MediaKind\` enum is named \`DbMediaKind\` to avoid colliding with SP1's \`MediaKind\` oneof."). Keep every `// ── Batch N …` banner + every message/enum block verbatim.
  3. **FQN-drop in the append block:** replace every `media.v1.<T>` → `<T>` (57 sites; types per inventory). Leave every `mediatime.v1.<T>` exactly as-is (7 sites: 4 `TimeRange`, 3 `Timebase`). The 2 comment-only `media.v1.MediaKind` strings (orig lines 14, 34) become bare `MediaKind` — but since the SP1-vs-SP2 `MediaKind` distinction is exactly what changes, rewrite those two comment fragments to reference `DbMediaKind` (e.g. line 34 "Database asset tri-state. DISTINCT from media.v1.MediaKind (the SP1 …)" → "Database asset tri-state. Renamed \`DbMediaKind\` (distinct from SP1's \`MediaKind\` oneof; same package since mono-consolidation)").
  4. **Rename #1 in the append block:** `enum MediaKind {` → `enum DbMediaKind {` (orig line 36); the field use `MediaKind kind = 2;` in `message Media` (orig line 396) → `DbMediaKind kind = 2;`. Enum **values** `MEDIA_KIND_UNSPECIFIED/_VIDEO/_AUDIO` are UNCHANGED. (Confirm no other bare `MediaKind` token exists in the SP2 block: the grep `grep -nE '(^|[^.a-zA-Z])MediaKind' /tmp/mono-docs/database.proto.orig` returns exactly the def line, the `Media.kind` use, and the 2 comment lines handled in sub-step 3 — nothing else.)
  5. **Append** the transformed block to `proto/media/v1/types.proto` after SP1's final message (`message HumanAnalysis { … }` ends the SP1 file at line 295–296) — add one blank line then the transformed SP2 header comment + body.
  6. **Delete** the old proto + prune dir: `git -C /Users/user/Develop/findit-studio/mediaschema rm proto/findit/db/v1/database.proto` then `rmdir -p proto/findit/db/v1 proto/findit/db proto/findit 2>/dev/null || true` (rmdir only removes if empty; `proto/findit` should be empty after — confirm `find proto/findit -type f` is empty / dir gone).
  7. **xtask single-entry `.files()`** in `xtask/src/main.rs` — replace the current 3-entry block (lines 26–30):
     ```rust
             .files(&[
                 root.join("proto/media/v1/types.proto"),
                 root.join("proto/findit/db/v1/database.proto"),
                 root.join("proto/findit/net/v1/network.proto"),
             ])
     ```
     with the single entry:
     ```rust
             .files(&[root.join("proto/media/v1/types.proto")])
     ```
     **Note:** `sp1-common`'s `xtask/src/main.rs` is ALREADY single-entry (`.files(&[root.join("proto/media/v1/types.proto")])`) — after the `reset --hard sp1-common` the file is already correct. **Verify** `grep -n 'files(' xtask/src/main.rs` shows the single entry; if so, NO xtask edit is needed in M2 (the 3-entry form only exists on the old sp2/sp3 tips that were reset away). Record "xtask already single-entry post-reset — no edit" if confirmed.
  8. **Regenerate:** `env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo cargo run -p xtask -- gen` (dangerouslyDisableSandbox:true). Expect `generated -> …/src/generated`. Then **delete stale generated files**: `git -C /Users/user/Develop/findit-studio/mediaschema rm src/generated/findit.db.v1.database.rs src/generated/findit.db.v1.database.__oneof.rs src/generated/findit.db.v1.mod.rs` if they were re-created they would not be (no `findit.db.v1` package compiles now) — but they exist in the reset tree only if `sp1-common` had them (it does NOT — `sp1-common` predates SP2). After `reset --hard sp1-common` the generated dir has only `media.v1.*` + `mod.rs`; the regen ADDS the SP2 messages into `src/generated/media.v1.types.rs` (+ `media.v1.types.__oneof.rs` for `SubtitleTrackOrigin.source`) and updates `mod.rs`. Confirm: `ls src/generated` shows ONLY `media.v1.mod.rs`, `media.v1.types.rs`, `media.v1.types.__oneof.rs`, `mod.rs` — **NO `findit.*`**, no `mediatime.v1.*`, no `*__view*`. (If any `findit.db.v1.*` file is present it is stale from a mis-reset — `git rm` it and re-verify the reset was to `sp1-common`.)
  9. **`src/lib.rs` — collapse for the SP2 slice.** After `reset --hard sp1-common`, `src/lib.rs` has ONLY the SP1 `media.v1` block (the `pub use generated::media::v1::{ … };` named list + the 3 SP1 oneof `pub use` lines: `media_kind::Kind as MediaKindKind`, `location::Kind as LocationKind`, `location_target::Kind as LocationTargetKind`). Merge the SP2 idents INTO that single brace list (alphabetically), and add the SP2 oneof companion line — do NOT create a second `pub use` block:
     - Add to the merged `pub use generated::media::v1::{ … };` list (alphabetical) every SP2 message/enum ident that the original SP2 lib block exported, **with `MediaKind`→`DbMediaKind`** as a plain ident (NOT `as`-aliased — it is now the real generated name): `Audio, AudioAnalysis, AudioChannelLayout, AudioChannelOrderKind, AudioChannelSpec, AudioClipKind, AudioCodec, AudioContainerFormat, AudioCoverArt, AudioEvent, AudioFileRecord, AudioMeta, AudioPrefilterClass, AudioSampleFormat, AudioStreamMeta, AudioSummary, AudioTrack, AudioTrackMeta, AudioTrackRole, AudioTranscriptSegment, Ced, CedDetection, ChannelLayoutKind, Chromaprint, Clap, DbMediaKind, Ebur128, FailedFile, Keyframe, Media, MediaMeta, Scene, SceneMeta, SceneVlmResult, SoundSource, Sp2CodegenSmoke, SpeakerSegment, Subtitle, SubtitleCue, SubtitleMeta, SubtitleTrack, SubtitleTrackFormat, SubtitleTrackMeta, SubtitleTrackOrigin, SubtitleTrackRole, TagConfidence, Timecode, TrackClassificationType, TrackRecord, TrackTag, Video, VideoMeta, VideoStreamMeta, VideoTrack, VideoTrackMeta` (these are SP2's owned types; `FailedFile` here is SP2's DB record, kept name). Merge them into the existing SP1 list so the final brace list is one ASCII-sorted set of SP0+SP1+SP2 idents.
     - Replace the old `pub use generated::findit::db::v1::MediaKind as DbMediaKind;` line with: nothing (the ident is now in the merged list as plain `DbMediaKind`). Update its doc-comment to `/// Database asset tri-state (renamed from the pre-consolidation \`findit.db.v1.MediaKind\`; distinct from SP1's [\`MediaKind\`] oneof — same \`media.v1\` package since the mono-consolidation).` placed as a `//` comment above the merged list or dropped (the rename is self-documenting; keep a one-line note near the list head).
     - Re-point the SP2 oneof companion: `pub use generated::findit::db::v1::subtitle_track_origin::Source as SubtitleTrackOriginSource;` → `pub use generated::media::v1::subtitle_track_origin::Source as SubtitleTrackOriginSource;` (keep its doc comment). Place it with the other oneof `pub use` lines (after the merged brace list, alongside `MediaKindKind`/`LocationKind`/`LocationTargetKind`).
     - Delete the entire `// SP2 — findit \`database\` package (\`findit.db.v1\`). …` comment block + its `pub use generated::findit::db::v1::{ … };` block (now subsumed). Keep one short `// SP2 database-domain types (same media.v1 package since mono-consolidation).` comment if helpful.
  10. **`tests/roundtrip.rs` — verify unchanged-by-merge.** The SP2 test bodies construct via `mediaschema::<Type>` (e.g. `DbMediaKind::MEDIA_KIND_VIDEO`, `FailedFile{…}`, `SubtitleTrackOriginSource::…`) — all still resolve (the crate-root names are identical; `DbMediaKind` was already the alias name, now the real name). The `use mediaschema::{…}` import block already lists `DbMediaKind` and SP2's `FailedFile` and `SubtitleTrackOriginSource` — **no import-block change** (no ident added/removed by the SP2 merge: the rename target `DbMediaKind` already existed as the alias name). **Confirm** by grep that NO test references a `findit::db::v1` path or a now-removed `mediaschema::MediaKind`-meaning-the-SP2-enum (the SP2 enum was always referenced as `DbMediaKind`): `grep -nE 'findit::db::v1|findit\.db\.v1' tests/roundtrip.rs` → no matches. The ASCII-sorted import block stays sorted (it already is). Only touch `tests/roundtrip.rs` if a build/test failure proves a real reference broke (it should not — capture evidence before any edit).
- [ ] **Step 5 — verification (C2 + C3 + C4 on `sp2-database`).** Run the C2 block. All 6 builds PASS; `cargo test --features quickcheck,json` shows the integration `roundtrip` count == the Step-1 baseline (**40**: SP1 19 + SP2 21 — exactly preserved, structural refactor); `GEN_CLEAN`. Run the C4 grep guards adapted for the SP2 stage: guard (a) `findit.db.v1`/`findit::db::v1` absent everywhere; guard (b) no `database.proto`, no `src/generated/findit.*`; guard (c) exactly one `package `/`syntax ` line in `types.proto`; guard (d) `^enum DbMediaKind \{` present once, `^enum MediaKind \{` absent (SP1's is `message MediaKind`), `^message MediaKind \{` present once, `^message FailedFile \{` present once (SP2's kept DB record); guard (e) `media.v1.`/`findit.db.v1.` qualifiers absent from `types.proto`, `mediatime.v1.` count == 7 at this stage (SP3 not merged yet). **Note:** SP3-only guards (`NetFailedFile`, `findit.net.v1`) are deferred to M3 — at the M2 stage `findit.net.v1` legitimately does not exist yet and `network.proto` is not yet present (it is added in M3); do NOT assert SP3 invariants here.
- [ ] **Step 6 — ONE consolidation commit on `sp2-database`.**
  ```
  cd /Users/user/Develop/findit-studio/mediaschema
  git add proto/media/v1/types.proto src/generated src/lib.rs xtask/src/main.rs tests/roundtrip.rs
  git add -A proto/findit 2>/dev/null || true   # stage the deletion/dir prune
  git status   # confirm: types.proto modified, database.proto deleted, generated updated, lib modified, NO findit.db.v1.* present
  git commit -m "$(printf 'refactor(sp2): consolidate findit.{db,net}.v1 → media.v1 (mono single-file package)\n\nStructural refactor (no field/tag/enum/message changed): SP2 database types\nmoved into the single media.v1 package / proto/media/v1/types.proto;\nfindit.db.v1.MediaKind renamed DbMediaKind (same-package collision with\nSP1 MediaKind oneof); all cross-package media.v1.* refs are now bare\nsame-package; xtask .files() single-entry; generated regenerated\n(media.v1.* only). Per explicit user directive.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>')"
  ```
  (Commit message subject is the directive's exact `refactor(spX): consolidate findit.{db,net}.v1 → media.v1 (mono single-file package)` form.)
- [ ] **Step 7 — two-stage review (C6).** Dispatch a subagent: stage 1 (spec/structure fidelity) — diff the new `types.proto` SP2 region against `0ddb1cc:proto/findit/db/v1/database.proto` and assert byte-equality of every message/enum/field/tag/`reserved` EXCEPT (i) the dropped `syntax`/`package`/`import` header lines, (ii) `media.v1.X`→`X` on the 57 sites, (iii) `MediaKind`→`DbMediaKind` at the def + `Media.kind` use + 2 comments, (iv) the retitled header comment; confirm NO content drift, `mediatime.v1.*` untouched. Stage 2 (code-quality) — generated drift-clean, `src/lib.rs` has exactly ONE `pub use generated::media::v1::{…}` brace list (SP0+SP1+SP2 idents, ASCII-sorted, `DbMediaKind` plain not aliased), oneof companions re-pointed, tests still 40, C4 guards (a)–(e) hold. Triage findings per C6; fix inline; re-run C2/C4; amend the consolidation commit (`git commit --amend --no-edit`) if a fix was applied.
- [ ] **Step 8 — push (force-with-lease).** `git -C /Users/user/Develop/findit-studio/mediaschema push --force-with-lease origin sp2-database`. PR #4 (`sp2-database`→`sp1-common`) auto-updates (base unchanged). Confirm `gh pr view 4 --repo Findit-AI/mediaschema --json headRefOid,baseRefName` shows the new head SHA and `baseRefName == "sp1-common"`.

**M2 task/step count: 1 task, 9 steps (Step 0–8; Step 4 has 10 sub-steps).**

---

## Task M3: Consolidate SP3 — rebuild `sp3-network` (SP3 slice of steps 1–7)

**Goal:** rebuild `sp3-network` = the NEW `sp2-database` (post-M2) + the reconciled SP3 docs commits + the mono-consolidation plan doc + ONE consolidation commit applying the SP3 slice (NetFailedFile rename, SP3 content into `types.proto`, drop FQNs, regen, lib collapse for SP3 idents).

**Files (in the rebuilt tree):** `proto/media/v1/types.proto` (append SP3 body after the SP2 region); delete `proto/findit/net/v1/network.proto` (+ prune dir); `src/lib.rs` (merge SP3 idents into the single `media.v1` block); `src/generated/**` (regen); `tests/roundtrip.rs` (verify unchanged-by-merge); the reconciled SP3 plan doc + this mono-consolidation plan doc.

- [ ] **Step 1 — capture the SP3 baseline test count.** Before resetting, with the ORIGINAL `sp3-network`@`9dbdec6`: the known integration `roundtrip` count is **60** (SP1 19 + SP2 21 + SP3 20). (If desired, re-confirm by `git checkout 9dbdec6 --detach` + `cargo test --features quickcheck,json 2>&1 | grep "test result:"`, then return.) This is the post-rebuild target for `sp3-network` (C3).
- [ ] **Step 2 — reset `sp3-network` onto the rebuilt `sp2-database`.**
  ```
  git -C /Users/user/Develop/findit-studio/mediaschema checkout sp3-network
  git -C /Users/user/Develop/findit-studio/mediaschema reset --hard sp2-database
  ```
  (`sp2-database` is now the post-M2 mono tip. Verify `git rev-parse HEAD` == post-M2 `sp2-database` SHA; confirm `proto/media/v1/types.proto` contains the SP2 region (`grep -c '^message ' proto/media/v1/types.proto` ≈ 46 SP1 + 43 SP2 = 89; `grep -nE '^enum DbMediaKind' proto/media/v1/types.proto` present) and `proto/findit/` does NOT exist.)
- [ ] **Step 3 — re-add the reconciled SP3 docs commits + the mono plan doc.** The original SP3 had 4 docs commits (`3080f17` design, `e54be45` plan, `e9b502c` §7-count reconcile, `594d9d8` §7.8 reconcile). Since the spec was already reconciled+committed on `sp2-database` in M2 (shared file), the §7/§7.8 spec edits are ALREADY present in the reset tree — do NOT re-commit the spec. Re-add only the SP3 plan doc + the mono-consolidation plan doc:
  - Commit A (SP3 plan):
    ```
    cp /tmp/mono-docs/2026-05-18-mediaschema-sp3-network.md docs/superpowers/plans/2026-05-18-mediaschema-sp3-network.md
    git add docs/superpowers/plans/2026-05-18-mediaschema-sp3-network.md
    git commit -m "$(printf 'docs: SP3 implementation plan (mono-consolidated; single media.v1 file)\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>')"
    ```
  - Commit B (this mono-consolidation plan — re-add it onto the rebuilt branch; the scaffolding commit made on the old `sp3-network` is superseded by the reset, expected/fine):
    ```
    cp /tmp/mono-docs/2026-05-18-mediaschema-mono-consolidation.md docs/superpowers/plans/2026-05-18-mediaschema-mono-consolidation.md
    git add docs/superpowers/plans/2026-05-18-mediaschema-mono-consolidation.md
    git commit -m "$(printf 'docs: mono-consolidation plan (findit.{db,net}.v1 → single media.v1 file)\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>')"
    ```
  (The reconciled spec already rides `sp2-database`; the SP3 design-doc content lives in §7 of that shared spec, so a separate "SP3 design" commit re-add is not needed — its content reconciliation is part of M1's spec edit committed in M2. If a reviewer expects a distinct SP3-design commit for PR #5 history parity, that is acceptable to omit: the spec is shared and its §7 is on the base branch — note this as a resolved ambiguity, see M4 report.)
- [ ] **Step 4 — apply the SP3 consolidation transform** (one working-tree edit set, then ONE commit):
  1. **Read** the SP3 proto body from the ORIGINAL `sp3-network`@`9dbdec6`: `git -C /Users/user/Develop/findit-studio/mediaschema show 9dbdec6:proto/findit/net/v1/network.proto > /tmp/mono-docs/network.proto.orig`.
  2. **Build the SP3 append block** from `network.proto.orig` (per C7): drop lines 1–3 (`syntax`/blank/`package findit.net.v1;`), the three `import` lines (`media/v1/types.proto`, `findit/db/v1/database.proto`, `mediatime/v1/mediatime.proto` — orig lines 22–24) and surrounding blanks. Keep the header comment block, retitled `// ── SP3: findit \`network\` domain (consolidated into media.v1) ──`, and reword the "REUSE, NEVER REDEFINE … buffa compiles this file together with proto/media/v1/types.proto AND proto/findit/db/v1/database.proto … findit.net.v1.FailedFile is a DIFFERENT type from findit.db.v1.FailedFile" sentence to: "Same-package (media.v1) since the mono-consolidation; the SP3 \`FailedFile\` message is named \`NetFailedFile\` to avoid colliding with SP2's \`FailedFile\` DB record." Keep every `// ── Batch N …` banner + every message block verbatim.
  3. **FQN-drop in the append block:** every `media.v1.<T>` → `<T>` (25 sites) and every `findit.db.v1.<T>` → `<T>` (5 field sites: `Sp3CodegenSmoke.video_meta`→`VideoMeta`, `BrowseItem.meta`→`VideoMeta`, `GetIndexedFileResponse.video`→`Video`, `GetIndexedFileResponse.scenes`→`Scene`; the 3 comment-only `findit.db.v1.…` strings at orig lines 20/117/237 reworded same-package). Leave `mediatime.v1.TimeRange` (4 sites) exactly as-is.
  4. **Rename #2 in the append block:** `message FailedFile {` → `message NetFailedFile {` (orig line 118); the 2 field uses `repeated FailedFile failed_files = 6;` (in `GetLocationStatsResponse`, orig line 216) and `repeated FailedFile failed_files = 2;` (in `FailedFilesResponse`, orig line 222) → `repeated NetFailedFile failed_files = …;`. (Confirm no other bare `FailedFile` token in the SP3 block: `grep -nE '(^|[^.a-zA-Z])FailedFile[^s]' /tmp/mono-docs/network.proto.orig` returns the def + the 2 `failed_files` uses + the 3 reworded `findit.db.v1.FailedFile` comments only — note `FailedFilesResponse` is a DIFFERENT name, NOT renamed; the negative-lookahead-ish `[^s]` avoids matching `FailedFilesResponse`.)
  5. **Append** the transformed SP3 block to `proto/media/v1/types.proto` after the SP2 region's final message (`message AudioFileRecord { … }`, which is the last SP2 message) — one blank line then the transformed SP3 header comment + body.
  6. **Delete** the old proto + prune dir: `git -C /Users/user/Develop/findit-studio/mediaschema rm proto/findit/net/v1/network.proto` then `rmdir -p proto/findit/net/v1 proto/findit/net proto/findit 2>/dev/null || true` (confirm `proto/findit` gone / empty; SP2 already removed `proto/findit/db/...` in M2, so this removes the last `proto/findit/...` content).
  7. **xtask:** already single-entry from M2 (the reset to `sp2-database` carried M2's single-entry `xtask/src/main.rs`). **Verify** `grep -n 'files(' xtask/src/main.rs` → `.files(&[root.join("proto/media/v1/types.proto")])`; NO edit needed (record "xtask already single-entry from M2").
  8. **Regenerate:** `env CARGO_HOME=/Users/user/Develop/findit-studio/.cargo cargo run -p xtask -- gen` (dangerouslyDisableSandbox:true). Expect `generated -> …`. Confirm `ls src/generated` shows ONLY `media.v1.mod.rs`, `media.v1.types.rs`, `media.v1.types.__oneof.rs`, `mod.rs` — NO `findit.*`, no `mediatime.v1.*`, no `*__view*`. (The SP3 messages — incl. the `Request`/`Response`/`Event` oneofs — are emitted into `media.v1.types.rs` + `media.v1.types.__oneof.rs`; `mod.rs` updated.) If any `findit.net.v1.*` file exists it is stale — `git rm` it (it cannot regenerate; no `findit.net.v1` package compiles).
  9. **`src/lib.rs` — collapse for the SP3 slice.** Merge SP3's owned idents INTO the single `pub use generated::media::v1::{ … };` brace list (alphabetically, with the SP2 idents from M2 already there), **with `FailedFile`→`NetFailedFile`** as a plain ident (NOT `as`-aliased — now the real generated name): add `BrowseItem, BrowseRequest, BrowseResponse, EjectVolumeRequest, EjectVolumeResponse, Event, FailedFilesResponse, FolderUpdatedEvent, GetDaemonInfoRequest, GetDaemonInfoResponse, GetFileIndexingStatsRequest, GetFileIndexingStatsResponse, GetIndexedFileRequest, GetIndexedFileResponse, GetLocationStatsRequest, GetLocationStatsResponse, GetModelStatusRequest, GetModelStatusResponse, HeartbeatRequest, HeartbeatResponse, IndexLocationRequest, IndexLocationResponse, IndexingFile, IndexingProgressResponse, ListLocationsRequest, ListLocationsResponse, ModelDownloadProgress, ModelDownloadProgressEvent, ModelDownloadProgressResponse, ModelInfo, NetFailedFile, Pagination, RemoveLocationRequest, RemoveLocationResponse, Request, RequestEnvelope, Response, ResponseEnvelope, RetryFailedRequest, RetryFailedResponse, SearchFilter, SearchHit, SearchRequest, SearchResponse, Sp3CodegenSmoke, UpdateAnnotationRequest, UpdateAnnotationResponse, Volume, VolumeStateChangedEvent` into the one ASCII-sorted brace list (now SP0+SP1+SP2+SP3).
     - Delete the old `pub use generated::findit::net::v1::FailedFile as NetFailedFile;` line (the ident is now plain `NetFailedFile` in the merged list). Keep a one-line `//` note: `// SP3's FailedFile renamed NetFailedFile (same media.v1 package; distinct from SP2 FailedFile DB record).`
     - Re-point the 3 SP3 oneof companions to `generated::media::v1::…`:
       - `pub use generated::findit::net::v1::__buffa::oneof::request::Kind as RequestKind;` → `pub use generated::media::v1::__buffa::oneof::request::Kind as RequestKind;`
       - `… response::Kind as ResponseKind;` → `pub use generated::media::v1::__buffa::oneof::response::Kind as ResponseKind;`
       - `… event::Kind as EventKind;` → `pub use generated::media::v1::__buffa::oneof::event::Kind as EventKind;`
       Keep their doc comments verbatim. **However:** the exact generated oneof path may be `generated::media::v1::request::Kind` (per-message module) rather than `__buffa::oneof::request::Kind` — the SP1/SP2 oneof companions use the per-message-module form (`media_kind::Kind`, `subtitle_track_origin::Source`). **Confirm the real path from the regenerated source** before finalizing: `grep -nE 'pub mod request|pub mod response|pub mod event|mod __buffa|pub enum Kind' src/generated/media.v1.types*.rs` and match the exact module path buffa emits for the `Request`/`Response`/`Event` oneofs in the `media.v1` package; use whichever path the regenerated `media.v1.types.rs` actually exposes (the original SP3 lib used `__buffa::oneof::request::Kind` — verify it is still that under `media.v1`, else use the per-message-module path). This is the one place the generated module path must be re-derived (it depends on buffa's emission for same-package oneofs); do it from the regenerated source, not from memory.
     - Delete the entire `// SP3 — findit \`network\` package (\`findit.net.v1\`). …` comment block + its `pub use generated::findit::net::v1::{ … };` block (subsumed). Keep one short `// SP3 network-domain types (same media.v1 package since mono-consolidation).` comment.
  10. **`tests/roundtrip.rs` — verify unchanged-by-merge.** SP3 test bodies construct via `mediaschema::<Type>` (`NetFailedFile{…}`, `Request{kind:Some(RequestKind::…)}`, `EventKind::FailedFiles(…)`, etc.) — all still resolve (`NetFailedFile` was already the alias name, now real; `RequestKind`/`ResponseKind`/`EventKind` re-pointed to the same logical oneof under `media.v1`). The `use mediaschema::{…}` import block already lists `NetFailedFile`, `Request`, `RequestKind`, `Response`, `ResponseKind`, `Event`, `EventKind` and SP3 messages — **no import-block change** (the SP3 merge adds/removes no crate-root ident: `NetFailedFile` already existed as the alias name). **Confirm**: `grep -nE 'findit::net::v1|findit\.net\.v1' tests/roundtrip.rs` → no matches. Only edit `tests/roundtrip.rs` if a real build/test failure proves a broken reference (capture evidence first).
- [ ] **Step 5 — verification (C2 + C3 + C4 full, on `sp3-network`).** Run C2. All 6 builds PASS; `cargo test --features quickcheck,json` integration `roundtrip` count == **60** (SP1 19 + SP2 21 + SP3 20 — exactly the Step-1 baseline, preserved); `GEN_CLEAN`. Run the FULL C4 grep guards (now SP3 invariants apply): (a) NO `findit.db.v1`/`findit.net.v1` (or `findit::db::v1`/`findit::net::v1`) anywhere in proto/src/tests; (b) NO `database.proto`/`network.proto`, NO `src/generated/findit.*`; (c) exactly one `package `/`syntax ` in `types.proto`; (d) `^enum DbMediaKind \{` ×1, `^message NetFailedFile \{` ×1, `^enum MediaKind \{` ×0, `^message MediaKind \{` ×1 (SP1 oneof kept), `^message FailedFile \{` ×1 (SP2 DB record kept), and the OLD colliding forms absent (`^message FailedFile \{` must be SP2's only — confirm there is exactly ONE `message FailedFile {` and exactly ONE `message NetFailedFile {`, never two `FailedFile`); (e) zero `media.v1.`/`findit.db.v1.`/`findit.net.v1.` qualifiers in `types.proto`, `mediatime.v1.` count == 11 (7 from SP2's TimeRange×4+Timebase×3 + 4 from SP3's TimeRange). Also spot-check the §7.9-style invariants on the merged generated file: `grep -rn 'ErrorResponse' src/generated/media.v1.types.rs` → none; `grep -n 'pub enum ' src/generated/media.v1.types.rs` → only legitimate proto enums incl. the renamed `DbMediaKind` and the `Request`/`Response`/`Event` oneof `Kind` enums, NO `findit.net.v1.ErrorResponse`; `grep -rn 'Header\|MessageType\|MessageFlags\|RequestId\|framing' src/generated/media.v1.types.rs` → none.
- [ ] **Step 6 — ONE consolidation commit on `sp3-network`.**
  ```
  cd /Users/user/Develop/findit-studio/mediaschema
  git add proto/media/v1/types.proto src/generated src/lib.rs xtask/src/main.rs tests/roundtrip.rs
  git add -A proto/findit 2>/dev/null || true
  git status   # types.proto modified, network.proto deleted, generated updated, lib modified, NO findit.net.v1.* present
  git commit -m "$(printf 'refactor(sp3): consolidate findit.{db,net}.v1 → media.v1 (mono single-file package)\n\nStructural refactor (no field/tag/enum/message changed): SP3 network types\nmoved into the single media.v1 package / proto/media/v1/types.proto;\nfindit.net.v1.FailedFile renamed NetFailedFile (same-package collision\nwith SP2 FailedFile DB record); all cross-package media.v1.* /\nfindit.db.v1.* refs are now bare same-package; xtask .files()\nsingle-entry; generated regenerated (media.v1.* only). Per explicit\nuser directive.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>')"
  ```
- [ ] **Step 7 — two-stage review (C6).** Subagent: stage 1 (spec/structure fidelity) — diff the new `types.proto` SP3 region against `9dbdec6:proto/findit/net/v1/network.proto`; assert byte-equality of every message/`oneof`/arm/tag (incl. `Response.error = 20000`, all `Request`/`Event` arm tags verbatim) EXCEPT (i) dropped header lines, (ii) `media.v1.X`→`X` (25) + `findit.db.v1.X`→`X` (5), (iii) `FailedFile`→`NetFailedFile` at def + 2 `failed_files` uses + 3 comments, (iv) retitled header; confirm NO content drift, `mediatime.v1.TimeRange` untouched, `FailedFilesResponse` NOT renamed. Stage 2 (code-quality) — generated drift-clean; `src/lib.rs` has exactly ONE `pub use generated::media::v1::{…}` brace list (SP0+SP1+SP2+SP3 ASCII-sorted, `DbMediaKind`+`NetFailedFile` plain), the 3 SP3 oneof companions + SP2's + SP1's all re-pointed under `media::v1::…` with the verified module path, tests still 60, full C4 guards hold. Triage per C6; fix inline; re-run C2/C4; `git commit --amend --no-edit` if fixed.
- [ ] **Step 8 — push (force-with-lease).** `git -C /Users/user/Develop/findit-studio/mediaschema push --force-with-lease origin sp3-network`. PR #5 (`sp3-network`→`sp2-database`) auto-updates (base unchanged). Confirm `gh pr view 5 --repo Findit-AI/mediaschema --json headRefOid,baseRefName` shows the new head SHA and `baseRefName == "sp2-database"`.

**M3 task/step count: 1 task, 8 steps (Step 1–8; Step 4 has 10 sub-steps).**

---

## Task M4: Finalize — whole-impl review + confirm PRs #4/#5 + report

**Files:** none (verification + PR confirmation + report only).

- [ ] **Step 1 — final whole-impl capstone review** over the rebuilt `sp3-network` tip (it transitively contains SP0+SP1 unchanged + the mono-consolidated SP2+SP3). Dispatch a subagent for a holistic pass: (a) the single `proto/media/v1/types.proto` is a valid one-package file — `grep -cE '^package |^syntax ' ` each ==1; the message/enum count == SP1(46 msg+5 enum) + SP2(43 msg, incl. `Sp2CodegenSmoke`; 12 enum incl. the renamed `DbMediaKind`) + SP3(49 msg incl. `Sp3CodegenSmoke` + the renamed `NetFailedFile`; 0 enum) = **138 messages + 17 enums** (`grep -cE '^message ' proto/media/v1/types.proto` == 138; `grep -cE '^enum ' ` == 17); (b) the full C4 guard set passes on `sp3-network`; (c) C2 fully green incl. **60** integration tests and `GEN_CLEAN`; (d) the two renames are correct everywhere (def + every use + generated + lib + tests); (e) `mediatime.v1` still extern (no `mediatime.v1.*` generated file; `extern_path` line intact in `xtask/src/main.rs`); (f) docs reconciled (M1) with no field/tag table altered. Triage any finding per C6.
- [ ] **Step 2 — confirm PR stack integrity.**
  ```
  gh pr view 1 --repo Findit-AI/mediaschema --json number,state,baseRefName,headRefName
  gh pr view 3 --repo Findit-AI/mediaschema --json number,state,baseRefName,headRefName
  gh pr view 4 --repo Findit-AI/mediaschema --json number,state,baseRefName,headRefOid,baseRefName
  gh pr view 5 --repo Findit-AI/mediaschema --json number,state,baseRefName,headRefOid,baseRefName
  ```
  Assert: #1 `sp0-foundation`→`main` OPEN unchanged; #3 `sp1-common`→`sp0-foundation` OPEN unchanged; #4 `sp2-database`→`sp1-common` OPEN, head == post-M2 SHA; #5 `sp3-network`→`sp2-database` OPEN, head == post-M3 SHA. Bases all unchanged (Q2=2a). Optionally add a short PR comment on #4/#5 noting the mono-consolidation refactor (structural; field/tag-preserving; per user directive) so reviewers see the rewrite rationale.
- [ ] **Step 3 — update the memory file.** Append to `/Users/user/.claude/projects/-Users-user-Develop-findit-studio-mediaschema/memory/mediaschema-buffa-migration.md` a line recording: mono-consolidation done — single `media.v1` package/`proto/media/v1/types.proto`, no cross-package refs, `findit.db.v1.MediaKind`→`DbMediaKind` + `findit.net.v1.FailedFile`→`NetFailedFile`, `sp2-database`/`sp3-network` force-rebuilt (PRs #4/#5 auto-updated, bases unchanged), 60 tests preserved, per user directive.
- [ ] **Step 4 — final report** (the M4 deliverable): plan path; M1–M4 task/step counts; the exact cross-package ref-site inventory & the 2 rename use-site counts; confirmation only `MediaKind`/`FailedFile` collide; the M2/M3 consolidation commit SHAs + the force-pushed branch HEADs; any ambiguity resolved with rationale (no TBDs).

**M4 task/step count: 1 task, 4 steps.**

---

## Self-Review (writing-plans)

**1. Every transform step (directive 1–10) is covered by a task/step.**
- Directive **1 (package+file merge)** → M2 Step 4.2/4.5 (append SP2 body, strip per-file header, keep the one `mediatime` import) + M3 Step 4.2/4.5 (append SP3 body) + M2/M3 Step 4.6 (delete old protos, prune `proto/findit/`). ✓
- Directive **2 (2 renames)** → M2 Step 4.4 (`MediaKind`→`DbMediaKind` def+`Media.kind`+comments) + M3 Step 4.4 (`FailedFile`→`NetFailedFile` def+2 `failed_files`+comments); regen carries them into `media.v1.types.rs` (M2/M3 Step 4.8); lib aliases removed → plain idents (M2 Step 4.9 / M3 Step 4.9); tests already use the alias names so bodies unchanged (M2 Step 4.10 / M3 Step 4.10). Collision survey (only these 2) stated in the pre-computed inventory and re-asserted in C4 guard (d). ✓
- Directive **3 (drop cross-package FQNs)** → M2 Step 4.3 (57 `media.v1.` in SP2) + M3 Step 4.3 (25 `media.v1.` + 5 `findit.db.v1.` in SP3); `mediatime.v1.` explicitly preserved (C7, the grep recipe, C4 guard (e) asserts `mediatime.v1.`==11 and zero other qualifiers). ✓
- Directive **4 (xtask single-entry)** → M2 Step 4.7 (no-op after `reset --hard sp1-common` which is already single-entry; verified, recorded) + M3 Step 4.7 (no-op, carried from M2; verified). ✓
- Directive **5 (regen → only media.v1.*)** → M2 Step 4.8 + M3 Step 4.8 (regen; assert `ls src/generated` has only `media.v1.*`+`mod.rs`, NO `findit.*`, NO `mediatime.v1.*`, NO `*__view*`; `Sp2/Sp3CodegenSmoke` retained as same-package guards). ✓
- Directive **6 (lib collapse)** → M2 Step 4.9 + M3 Step 4.9 (ONE merged `pub use generated::media::v1::{…}` ASCII-sorted brace list; `DbMediaKind`/`NetFailedFile` plain; SP1+SP2+SP3 oneof companions re-pointed under `media::v1::…`, with the explicit "verify the real generated oneof module path from regenerated source" instruction — the one path that legitimately must be re-derived). ✓
- Directive **7 (tests barely change)** → M2 Step 4.10 + M3 Step 4.10 (verify-unchanged; import block unchanged because both rename targets pre-existed as the alias names; grep no `findit::{db,net}::v1`; only edit on proven failure with evidence). ✓
- Directive **8 (branch rebuild, Q2=2a in place)** → M2 Steps 2/3/6/8 (reset to `sp1-common`, re-add 2 docs commits verbatim-style, ONE consolidation commit with the exact `refactor(sp2): consolidate…` message, `--force-with-lease`) + M3 Steps 2/3/6/8 (reset to rebuilt `sp2-database`, re-add SP3 plan + mono plan docs, ONE `refactor(sp3): consolidate…` commit, `--force-with-lease`); PR bases unchanged (#4→`sp1-common`, #5→`sp2-database`), confirmed in M4 Step 2. `sp1-common`/#1/#3 untouched (stated in Architecture + M2 Step 2). ✓
- Directive **9 (doc reconcile)** → M1 Steps 1–7 (spec §2/§3/§6.1/§6.8/§7.1/§7.8 + the §6.8/§7.8 self-correction notes + SP2/SP3 plan headers; §1–§5 + §6.5/§7.5 mapping tables explicitly UNCHANGED; consistency grep in Step 7). ✓
- Directive **10 (verification every consolidation commit)** → C2 (6 builds + `cargo test --features quickcheck,json` + GEN_CLEAN) + C3 (exact count: 40 on sp2, 60 on sp3 — preserved) + C4 (the full grep-guard set incl. exactly-one-`package`, both renames present/old-collisions-absent, no `findit.*`, no old proto files) applied in M2 Step 5 and M3 Step 5; M4 Step 1 capstone re-runs them whole-impl. ✓

**2. Placeholder scan.** No "TBD", no "see spec", no "handle the rest", no "…(other fields)". Every cross-package site count is concrete (57 / 25 / 5) with per-type breakdowns; both rename use-site lists are enumerated with exact file:line anchors from the read of `0ddb1cc`/`9dbdec6` content; the exact append insertion points (after `HumanAnalysis` for SP2, after `AudioFileRecord` for SP3) are named; the exact xtask block to look for is quoted; the exact commit messages (with the directive's mandated subject form + Co-Authored-By) are spelled out; the verification/guard blocks are literal runnable commands with `dangerouslyDisableSandbox:true` + `CARGO_HOME`. The single intentionally-deferred derivation (the regenerated same-package oneof module path for `RequestKind`/`ResponseKind`/`EventKind`) is explicitly flagged with the exact grep to resolve it from the regenerated source — not a placeholder but a "derive from generated output, here is how" instruction (the content is generated, so it cannot be pre-stated; this is correct per C1). ✓

**3. Rename / FQN / branch-rebuild consistency.**
- The 2 renames are applied at definition AND every use site AND via regen AND in lib (alias→plain) AND verified in tests, in BOTH M2 (DbMediaKind) and M3 (NetFailedFile); SP1's `MediaKind` (message/oneof) and SP2's `FailedFile` (DB record) are explicitly NOT renamed and are asserted present by C4 guard (d) (`^message MediaKind`×1, `^message FailedFile`×1). `FailedFilesResponse` is explicitly called out as NOT renamed (M3 Step 4.4 negative match). ✓
- FQN-drop rule is stated once (Conventions/inventory) with a mechanical grep recipe and the ordering constraint ("apply renames AFTER FQN-drop" so SP1's bare `MediaKind` is not caught by the SP2 rename pass; the SP2 pass runs only over the appended SP2 block which has no `media.v1.MediaKind` field). `mediatime.v1.` invariance is repeated in C7, the recipe, and C4 guard (e). ✓
- Branch rebuild is internally consistent: M2 resets `sp2-database`→`sp1-common` (unchanged base), M3 resets `sp3-network`→the NEW `sp2-database`; docs commits are re-added before the consolidation commit on each; the shared spec is committed only once (in M2, on `sp2-database`) and is already present in M3's reset tree (M3 Step 3 explicitly does NOT re-commit the spec — avoids a double spec commit / divergent spec); `--force-with-lease` only (C5), local==origin precondition verified at plan start; PR bases never changed; the plan-doc commit on the old `sp3-network` being superseded by M3's reset is explicitly called out as expected and the mono plan doc is re-added in M3 Step 3 Commit B. The M1→M2 spec-carry hazard (branch reset would lose M1's in-tree edits) is handled by M2 Step 0 (copy to `/tmp/mono-docs/` outside the repo). ✓
- Resolved ambiguities (no TBDs): (i) xtask edit is a **no-op** post-`reset --hard sp1-common` because `sp1-common` is already single-`.files()` — handled by "verify, record no-op" rather than a blind edit (rationale: the 3-entry form only ever existed on the reset-away sp2/sp3 tips). (ii) The SP3 "design doc" commit is NOT separately re-added on `sp3-network` because the SP3 design is §7 of the SHARED spec already committed on `sp2-database` (the base) — re-committing it on `sp3-network` would duplicate/diverge the shared spec; documented as a resolved ambiguity (M3 Step 3 + M4 report). (iii) The regenerated same-package oneof module path (`__buffa::oneof::request::Kind` vs per-message `request::Kind`) is resolved from the regenerated source at M3 Step 4.9, not guessed (buffa's same-package emission determines it; the SP1/SP2 companions use the per-message-module form, so the SP3 form must be re-verified). ✓
