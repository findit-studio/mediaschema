[workspace]
members = ["mediaschema-derive", "xtask"]

[package]
name = "mediaschema"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Product-agnostic media-primitive schema (buffa-generated)"
publish = false
rust-version = "1.85"

[features]
default = ["std"]
std = ["buffa/std"]
serde = ["dep:serde", "dep:serde_json", "buffa/json", "buffa-types/json"]
arbitrary = ["dep:arbitrary", "buffa/arbitrary"]
quickcheck = ["arbitrary", "dep:quickcheck", "dep:mediaschema-derive"]

[dependencies]
buffa = { version = "0.6", default-features = false }
buffa-types = { version = "0.6", default-features = false }
mediatime = { path = "../mediatime", default-features = false, features = ["buffa"] }
serde = { version = "1", optional = true }
serde_json = { version = "1", optional = true }
arbitrary = { version = "1", optional = true, features = ["derive"] }
quickcheck = { version = "1", optional = true }
mediaschema-derive = { path = "mediaschema-derive", optional = true }

[dev-dependencies]
quickcheck = "1"
serde_json = "1"

[lints.rust]
rust_2018_idioms = "warn"
